import styled from "styled-components";
import { Button } from "@mui/material";

export const ButtonMui = styled(Button) `
    border-color: #448b2a !important;
    color: #18230b !important;
    text-transform: none !important;
`